# grsfd — core, design-system reference

The production core dashboard is already coded elsewhere. This folder intentionally does **not** mock it.

Instead, `index.html` is a single-page reference for aligning the real app with the shared design system: tokens, type roles, button variants, chip pills, and a handoff snippet. Use it as a checklist when wiring styles into the real codebase.

**Files**
- `index.html` — design-system reference page

If the real app ever drifts, point the engineer here.
